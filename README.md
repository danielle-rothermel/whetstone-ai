# Whetstone

Whetstone composes the `dr-*` libraries into typed graph, provider-execution,
language-model, and evaluation-failure foundations.

Run the local checks with:

```sh
./scripts/ci/unit.sh
./scripts/ci/lint.sh
```
